<p id="notice"><%= notice %></p>

<div>

<h1>Welcome to Artifact Tracker!</h1>
<p>We are the single best place to store, share, and analyze your 3D artifacts! Please upload artifact files above, navigate to the "Tags" page to add extra metadata to your artifacts. You can also join or create groups via the "Groups" page to share your metadata and files with other researchers. You can find more people in the "Users" page.</p>

</div>

<h1>Artifacts</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Longitude</th>
      <th>Latitude</th>
      <th>Filepath</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @artifacts.each do |artifact| %>
      <tr>
        <td><%= artifact.name %></td>
        <td><%= artifact.description %></td>
        <td><%= artifact.longitude %></td>
        <td><%= artifact.latitude %></td>
        <td><%= artifact.filepath %></td>
        <td><button type="button" class="btn btn-default"><%= link_to 'Show', artifact %></button></td>
        <td><button type="button" class="btn btn-success"><%= link_to 'Edit', edit_artifact_path(artifact) %></button></td>
        <td><button type="button" class="btn btn-danger"><%= link_to 'Destroy', artifact, method: :delete, data: { confirm: 'Are you sure?' } %></button></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<button type="button" class="btn btn-lg btn-primary"><%= link_to 'New Artifact', new_artifact_path %></button>